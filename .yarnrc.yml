nodeLinker: node-modules

# Yarn defaults publishing to registry.yarnpkg.com, which is a read-only mirror
# and answers PUT with a 404. Writes have to go to npm itself.
npmPublishRegistry: 'https://registry.npmjs.org'
